---
title: Format Jinja
footer: false
---

> [!TIP]
> Templates are reformatted by djlint, whose version is pinned on the `uvx` call in the workflow. Rules and profile come from `[tool.djlint]` in `pyproject.toml`. Findings it cannot fix on its own are reported by the `lint-jinja` job.
