---
title: Format CSS style
footer: false
labels: [🤖 ci]
---

> [!TIP]
> Stylelint applies `stylelint-config-standard` to CSS and `stylelint-config-sass-guidelines` to SCSS. Findings it cannot fix on its own are reported by the `lint-css` job.
