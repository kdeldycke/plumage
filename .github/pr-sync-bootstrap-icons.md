---
title: Sync Bootstrap Icons
footer: 'false'
---

> [!TIP]
> The icon font shipped by the `bootstrap-icons` release pinned in `plumage/package.json`, copied out of the npm package. The theme serves it from `/theme/fonts/` instead of a CDN, so this file has to move whenever Dependabot bumps that pin.
